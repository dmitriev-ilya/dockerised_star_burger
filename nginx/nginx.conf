server {

    listen 80;

    location / {
        include '/etc/nginx/proxy_params';
        proxy_pass http://127.0.0.1:8000;
    }

    location /media/ {
         alias /app/media/;
    }

    location /static/ {
         alias /app/staticfiles/;
    }

}